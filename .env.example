# PostgreSQL
DATABASE_URL=postgresql://lscala:lscala@localhost:5432/lscala

# API
API_PORT=4000
JWT_SECRET=change-me-lscala-atria-dev-secret
JWT_EXPIRES_IN=12h
# PWA móvil instalada (display-mode standalone). No aplica a Chrome de escritorio.
JWT_PERSISTENT_EXPIRES_IN=10y
CORS_ORIGIN=http://localhost:5173

# Superadmin oculto (migración 028). No aparece en listados de usuarias.
# Email: soporte@atria.cl
# Password temporal: AtriaSupport!2026  (debe cambiar al primer ingreso)
# Solo este usuario puede restablecer la contraseña del admin (owner).

# Web (Vite)
VITE_API_URL=http://localhost:4000

# Web Push (VAPID) — generar: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:soporte@atria.cl
